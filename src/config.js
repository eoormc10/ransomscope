// Paste your deployed Cloudflare Worker URL here (see worker/README.md),
// then commit + push. Until it's set, the LIVE FEED section shows a setup hint.
export const WORKER_URL = "https://ransomscope-proxy.eoormc.workers.dev";
