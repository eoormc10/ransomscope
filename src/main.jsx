import React from "react";
import ReactDOM from "react-dom/client";
import RansomScope from "./RansomScope.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RansomScope />
  </React.StrictMode>
);
